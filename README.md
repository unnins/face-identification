# face-identification
텐서플로우를 활용해 얼굴 인식을 하는 프로그램입니다.

main.py를 실행시키면 자동으로 얼굴 프레임을 찾아 지정된 폴더에 저장합니다. 이를 반복하여 인식할 사람 얼굴 데이터를 확보합니다.
running.py는 저장된 얼굴 데이터를 텐서플로우를 사용해서 학습합니다. 필요에 따라 세부 파라미터를 조정합니다.
result.py는 학습된 모델을 기반으로 얼굴 인식을 수행하고, 결과를 실시간으로 처리합니다. 얼굴에 바운딩 박스를 그리고 인식된 사람이 누구인지를 표시합니다.
